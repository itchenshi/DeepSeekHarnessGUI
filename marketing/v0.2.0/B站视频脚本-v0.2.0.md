# B 站视频脚本：DSH GUI 首次发布推广（约 4 分 30 秒）

> **定位**：本视频是 DSH GUI 在 B 站的**首次推广**——观众第一次听说这个项目，脚本从「什么是 DeepSeek Harness / 这个桌面壳解决什么问题」讲起，完整介绍产品，而不是更新增量。
> 标题 / 简介 / 标签见同目录《B站-DeepSeekHarnessGUI-v0.2.0.md》。
> 截图素材：同目录 `主窗口.png`、`设置.png`、`DSH设置.png`、`DSH侧边栏.png`（英文版同名 `-english.png`）。

## 基本信息

- **时长目标**：4:10 – 4:45
- **录制**：OBS 录屏（1080p）+ 系统麦克风；素材为 Windows 实机（macOS / Linux 片段可后补）
- **BGM**：轻快科技感（剪辑软件自带 upbeat），转场处加「叮」音效
- **语速**：正常偏慢；技术名词（Harness、Electron、Node）首次出现时加字幕解释
- **开场要求**：**不要**「大家好我是 XX」式开场，前 5 秒必须有钩子

## 分镜脚本

| 时间 | 画面 | 口播（台词） | 字幕 / 花字 | 备注 |
|---|---|---|---|---|
| 0:00–0:15 | 黑场 → 3 帧快切：①浏览器开了 20 个标签页 ②终端里 `npm` 报错 ③关掉标签页后任务消失 | 「AI Agent 越来越能干活了，但用起来总觉得差一口气——任务跑在浏览器标签页里，关掉就没了；想升级，还得自己去敲命令。」 | 花字：引擎很强，体验差最后一公里 | 钩子开场，直戳痛点 |
| 0:15–0:50 | 切到 Harness 官网 / 终端 `dsh web` 界面 → 再切 DSH GUI 主窗口全屏 | 「这是 DeepSeek 开源的 Agent 框架 Harness。能力很强，它自带 Web UI——但只能开在浏览器里，没有桌面端。所以我给它写了一个开源的桌面壳，叫 **DSH GUI**。」 | 花字：DSH GUI · 开源 · 非官方<br>角标：GitHub / Gitee / GitCode | 建立认知：先说清 Harness 是什么 |
| 0:50–1:10 | 架构示意动画（可选）：官方 dsh 引擎 → DSH GUI 外壳 | 「先说清定位：它不是重写，也不是魔改。内核运行的永远是官方引擎，壳只负责官方没空做的『运行体验』——窗口、托盘、更新、数据目录。官方每次更新，它跟着躺赢。」 | 花字：内核 100% 官方，壳只管体验 | 打消「又一个 fork」的顾虑 |
| 1:10–1:55 | 下载页 → 下载便携 zip → 解压 → 双击运行 → 首次启动状态页「正在下载并安装…」进度条 → 进入主界面 | 「安装这块做得很省心：安装包自带便携 Node，普通用户**不用装 Node、不用碰命令行**，Windows、macOS、Linux 都有。第一次启动会自动联网装 Harness 引擎，一两分钟，之后打开就用。」 | 花字：零安装 · 三平台<br>角标：Windows / macOS / Linux | 零门槛是核心卖点，语速放缓 |
| 1:55–2:35 | 主窗口全屏操作：发一条任务、看 Agent 跑 → 关闭窗口 → 托盘图标 → 右键菜单「打开窗口 / 检查更新 / 设置 / 退出」→ 重新打开窗口 | 「用完关掉窗口，任务不会被杀掉——它待在系统托盘里，随时叫得回来。窗口用的是内存会话，cookies 和登录态不落盘，退出时整个进程树一起清理干净，每次打开都是全新的。」 | 花字：托盘常驻 · 内存会话 | 展示托盘 + 隐私特性 |
| 2:35–3:05 | 设置窗口 → 引擎更新区：更新策略（询问 / 静默 / 仅提示）→ 触发一次检查 → 右下角更新角标 | 「引擎更新是全自动的：启动时和运行中每 30 分钟检查一次 npm registry，发现新版按你的策略来——默认先问你一声，也可以静默更新或者只提示。更新完右下角会有角标告诉你。」 | 花字：自动保持最新引擎 | 三档策略要念清楚 |
| 3:05–3:55 | 设置窗「第三方插件」区 → 勾选「OpenCode 会话头」→ 重启引擎 → 引擎日志出现激活行 → 再演示「启动失败自愈」弹窗 | 「插件以前只能用命令行装，现在设置里**勾选即装**：插件市场、增强侧栏、智能体团队，都是核实过的。这个 OpenCode 会话头插件还做了安全加固——默认不透明 UUID，不会把你的内部会话 ID 发给第三方。万一把引擎搞崩了也不怕，它会自动撤掉插件并弹窗让你一键禁用重启。」 | 花字：插件勾选即装 · 安全加固 · 启动自愈 | 本段信息量大，可分两屏讲 |
| 3:55–4:15 | 设置窗语言：跟随系统 / 中文 / English → 切 English 页面即时变英文；再切 Harness 深色主题 → 壳窗口同步 | 「界面支持跟随系统、中文、English，切换**不用重启**，引擎那边是热发布的。主题也跟着 Harness 走——引擎深色，壳就是深色，不会一张深一张白。」 | 花字：多语言 · 主题一张皮 | 用英文截图 `设置-english.png` 对照 |
| 4:15–4:35 | 设置窗数据目录区 → 切换到应用目录 → 迁移进度 → 重启后新目录生效 | 「数据目录默认在 `~/.dsh`，想搬走也行：切到应用目录，它会停引擎、迁移、按新目录重启，一条龙。」 | 花字：数据可控可迁移 | 快速带过，别拖节奏 |
| 4:35–4:55 | 结尾：GitHub 仓库首页 + Releases 列表滚动 → 定格在仓库首页 | 「项目 MIT 开源，GitHub 主仓库，Gitee、GitCode 都有镜像，安装包以 GitHub Releases 为准，链接我放评论区置顶了。觉得有用点个三连，给仓库点个 Star；用出问题就来开 issue——开源项目最怕的不是批评，是沉默。下期见。」 | 置顶评论：GitHub 主仓库 + Gitee/GitCode 镜像 + 安装包链接 | 明确下一步动作 |

## 录制清单（B-roll 素材）

- [ ] 痛点三连帧：浏览器标签页 / 终端报错 / 任务消失（约 15s，可后期合成）
- [ ] Harness 官方 `dsh web` 界面（约 15s，用于对比）
- [ ] 下载 → 解压 → 双击运行 全过程（约 30s）
- [ ] 首次启动引擎安装进度页（约 40s，别删，第一次性体验亮点）
- [ ] 主窗口操作：建会话、发任务、看 Agent 跑（约 50s）
- [ ] 托盘右键菜单 + 关窗隐藏 + 恢复（约 25s）
- [ ] 设置窗：更新策略 / 插件区 / 语言 / 主题 / 数据目录（约 90s）
- [ ] 插件勾选 → 重启 → 引擎日志生效（约 30s）
- [ ] 启动失败自愈弹窗（约 20s）
- [ ] Releases 页面 + 仓库首页滚动（约 20s）

## 成片检查清单

- [ ] 前 5 秒有钩子（痛点三连帧），无「大家好我是」开场
- [ ] 讲清「内核 100% 官方、壳只管体验」，避免被误认为 fork
- [ ] 零安装（自带便携 Node）至少强调一次，这是最大的入门门槛差异
- [ ] 插件段点透「安全加固版：不透明 UUID，不泄露内部会话 ID」
- [ ] 三档更新策略、30 分钟间隔念准确（与 README 一致）
- [ ] 字幕完整（B 站大量静音观看）
- [ ] 全程无硬广感，安装包链接只出现在简介与置顶评论
- [ ] 结尾有明确下一步：三连 / Star / 提 issue
- [ ] 片尾置顶评论：GitHub 主仓库 + Gitee/GitCode 镜像 + 一句话「用出问题请开 issue」

## 附：一句话版视频简介（备用短文案）

> 给 DeepSeek Harness 写了个开源桌面壳：不占浏览器、自动更新引擎、插件勾选即装，安装包自带 Node 零门槛。Windows / macOS / Linux 都有。
> 项目：https://github.com/itchenshi/DeepSeekHarnessGUI
