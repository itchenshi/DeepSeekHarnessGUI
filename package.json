{
  "name": "dsh-gui-shell",
  "productName": "DSH GUI",
  "version": "0.1.0",
  "description": "App shell for DeepSeek Harness: embedded Web UI, always-latest engine, fresh session per launch",
  "author": "dsh-gui-shell contributors",
  "main": "src/main.js",
  "private": true,
  "scripts": {
    "start": "electron .",
    "make-icons": "node scripts/make-icons.mjs",
    "bundle:node": "node scripts/bundle-node.mjs",
    "fix:unpacked": "node scripts/fix-unpacked.mjs --force",
    "dist:win": "node scripts/make-icons.mjs && node scripts/bundle-node.mjs && electron-builder --win && node scripts/fix-unpacked.mjs --force",
    "dist:mac": "node scripts/make-icons.mjs && node scripts/bundle-node.mjs && electron-builder --mac && node scripts/fix-unpacked.mjs --force",
    "dist:linux": "node scripts/make-icons.mjs && node scripts/bundle-node.mjs && electron-builder --linux && node scripts/fix-unpacked.mjs --force",
    "dist": "node scripts/make-icons.mjs && node scripts/bundle-node.mjs && electron-builder --win --linux && node scripts/fix-unpacked.mjs --force"
  },
  "license": "MIT",
  "devDependencies": {
    "archiver": "^8.0.0",
    "electron": "^44.2.0",
    "electron-builder": "^26.15.3",
    "sharp": "^0.35.4"
  },
  "dependencies": {
    "semver": "^7.8.5",
    "yaml": "^2.9.0"
  }
}
